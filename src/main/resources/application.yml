spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: "jdbc:postgresql://localhost:5432/demo"
    username: postgres
    password: postgres
    properties:
       hibernate:
        format_sql: true